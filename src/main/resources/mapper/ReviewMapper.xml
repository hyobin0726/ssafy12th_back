<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.trip.enjoy_trip.repository.ReviewRepository">

    <!-- 리뷰 추가 -->
    <insert id="insertReview" useGeneratedKeys="true" keyProperty="reviewId">
        INSERT INTO review (content, point, visibility, user_id)
        VALUES (#{content}, #{point}, #{visibility}, #{userId})
    </insert>

    <!-- 리뷰 이미지 추가 -->
    <insert id="insertReviewImages">
        <foreach collection="imageUrls" item="imageUrl" index="index" open="" close="" separator="">
            INSERT INTO review_image (image_url, order_index, review_id)
            VALUES (#{imageUrl}, #{index}, #{reviewId})
        </foreach>
    </insert>


</mapper>
